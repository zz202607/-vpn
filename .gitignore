# OS junk files
Thumbs.db
Desktop.ini
.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp

# Secrets and subscription data — never commit these
*.key
*.pem
.env
subscription.txt
*.sub
config/*.yaml
!config/*.example.yaml

# Core binaries and archives (mihomo kernel is downloaded on demand)
core/*.exe
core/*.zip

# Logs
*.log

# Claude Code local settings
.claude/settings.local.json
